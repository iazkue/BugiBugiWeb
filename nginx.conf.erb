worker_processes 1;
daemon off;

events { worker_connections 1024; }

http {
  include mime.types;
  sendfile on;

  server {
    listen <%= ENV["PORT"] %>;
    server_name _;
    root /app;

    location / {
      try_files $uri $uri.html $uri/ =404;
    }
  }
}
